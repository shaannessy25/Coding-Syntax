import Linked_List from LinkedList

class Stack():
    def __init__():
        self.list = Linked_List()

    def push(newItem):
        selflist.prepent(newItem, idex = 0)

    def peek():
        if self.list.head != None:
            return self.list.head.data[0]
        else:
            return None

    def pop():
        top = self.peek()
        self.list.delete(top)
        return top



